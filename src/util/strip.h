#ifndef UTIL_STRIP_H_
#define UTIL_STRIP_H_

/** strip_raw_data
 * Strips the whitespace from the beginning of the specified raw data
 * @param data The raw data
 * @param len The length of the data
*/
char *strip_raw_data(char *data, unsigned int len);

#endif  // UTIL_STRIP_H_
